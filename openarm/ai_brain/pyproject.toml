[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openarm_ai_brain"
version = "0.1.0"
description = "openarm_ai_brain peppy node"
requires-python = ">=3.11,<3.14"
dependencies = ["peppygen", "peppylib"]

[project.scripts]
openarm_ai_brain = "openarm_ai_brain.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["src/openarm_ai_brain"]

[dependency-groups]
dev = ["pytest>=8", "pytest-asyncio>=0.25"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.uv.sources]
peppygen = { path = ".peppy/libs/peppygen" }
peppylib = { path = ".peppy/libs/peppylib" }